# Playing-card assets

The SVG card faces are based on the public-domain SVG-cards set by David Bellot.

Source reference: https://github.com/htdebeer/SVG-cards

The application uses the SVG card faces together with `J.png` for the joker and `CB.png` for the card back.
